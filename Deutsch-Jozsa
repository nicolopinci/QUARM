circuit = QuantumCircuit(numbit, numbit-1)
circuit.x(numbit-1)
circuit.barrier()
circuit.h(range(numbit))
circuit.barrier()
for i in range(numbit-1): circuit.cx(i, numbit-1) 
circuit.barrier()
circuit.h(range(numbit-1))
circuit.measure(range(numbit-1), range(numbit-1))
